export enum localStorageItems {
    playerId = 'playerId'
}

export enum pagesNames {
    loader = 'loader',
    login = 'login',
    profile = 'profile',
}

export enum dbCollections {
    players = 'players',
}

export enum statuses {
    online = 'online',
    offline = 'offline',
}